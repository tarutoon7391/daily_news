name: Daily AI News

on:
  schedule:
    - cron: "0 22 * * *"  # JST 7:00
  workflow_dispatch:

jobs:
  news:
    runs-on: ubuntu-latest

    steps:
      - name: Install tools
        run: sudo apt-get update && sudo apt-get install -y curl jq libxml2-utils

      # --- Fetch RSS ---

      - name: Fetch NHK Top News
        run: |
          HTTP_CODE=$(curl -s -o nhk.xml -w "%{http_code}" \
            -A "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36" \
            https://www3.nhk.or.jp/rss/news/cat0.xml)

          if [ "$HTTP_CODE" -ne 200 ] || ! xmllint --noout nhk.xml 2>/dev/null; then
            echo "NHK RSSの取得に失敗、スキップします..."
            echo "NHK_INPUT=NHK記事を取得できませんでした" >> $GITHUB_ENV
            exit 0
          fi

          NHK_TITLE=$(xmllint --xpath "string(//item[1]/title)" nhk.xml)
          NHK_DESC=$(xmllint --xpath "string(//item[1]/description)" nhk.xml)
          echo "NHK_INPUT<<EOF" >> $GITHUB_ENV
          echo "【${NHK_TITLE}】${NHK_DESC}" >> $GITHUB_ENV
          echo "EOF" >> $GITHUB_ENV

      - name: Fetch ITmedia AI+ News
        run: |
          HTTP_CODE=$(curl -s -o it_raw.xml -w "%{http_code}" \
            -A "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36" \
            https://rss.itmedia.co.jp/rss/2.0/aiplus.xml)

          if [ "$HTTP_CODE" -ne 200 ]; then
            echo "ITmedia AI+ RSSの取得に失敗（HTTP $HTTP_CODE）、スキップします..."
            echo "IT_INPUT=ITmedia AI+記事を取得できませんでした" >> $GITHUB_ENV
            exit 0
          fi

          DETECTED=$(file -bi it_raw.xml | grep -oP 'charset=\K[^ ;]+' || echo "utf-8")
          if [ "$DETECTED" != "utf-8" ] && [ "$DETECTED" != "us-ascii" ]; then
            iconv -f "$DETECTED" -t UTF-8 it_raw.xml > it.xml
          else
            mv it_raw.xml it.xml
          fi

          if ! xmllint --noout it.xml 2>/dev/null; then
            echo "ITmedia AI+のレスポンスが有効なXMLではありません、スキップします..."
            echo "IT_INPUT=ITmedia AI+記事を取得できませんでした" >> $GITHUB_ENV
            exit 0
          fi

          IT_TITLE=$(xmllint --xpath "string(//item[1]/title)" it.xml)
          IT_DESC=$(xmllint --xpath "string(//item[1]/description)" it.xml)
          echo "IT_INPUT<<EOF" >> $GITHUB_ENV
          echo "【${IT_TITLE}】${IT_DESC}" >> $GITHUB_ENV
          echo "EOF" >> $GITHUB_ENV

      - name: Fetch 4Gamer Game News
        run: |
          HTTP_CODE=$(curl -s -o game_raw.xml -w "%{http_code}" \
            -A "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36" \
            https://www.4gamer.net/rss/index.xml)

          if [ "$HTTP_CODE" -ne 200 ]; then
            echo "4Gamer RSSの取得に失敗（HTTP $HTTP_CODE）、スキップします..."
            echo "GAME_INPUT=4Gamer記事を取得できませんでした" >> $GITHUB_ENV
            exit 0
          fi

          DETECTED=$(file -bi game_raw.xml | grep -oP 'charset=\K[^ ;]+' || echo "utf-8")
          if [ "$DETECTED" != "utf-8" ] && [ "$DETECTED" != "us-ascii" ]; then
            iconv -f "$DETECTED" -t UTF-8 game_raw.xml > game.xml
          else
            mv game_raw.xml game.xml
          fi

          if ! xmllint --noout game.xml 2>/dev/null; then
            echo "4Gamerのレスポンスが有効なXMLではありません、スキップします..."
            echo "GAME_INPUT=4Gamer記事を取得できませんでした" >> $GITHUB_ENV
            exit 0
          fi

          GAME_TITLE=$(xmllint --xpath "string(//item[1]/title)" game.xml)
          GAME_DESC=$(xmllint --xpath "string(//item[1]/description)" game.xml)
          echo "GAME_INPUT<<EOF" >> $GITHUB_ENV
          echo "【${GAME_TITLE}】${GAME_DESC}" >> $GITHUB_ENV
          echo "EOF" >> $GITHUB_ENV

      # --- AI Summary ---

      - name: Summarize with AI
        env:
          API_KEY: ${{ secrets.COPILOT_API_KEY }}
        run: |
          summarize() {
            jq -n --arg content "$1" '{
              model: "gpt-4o-mini",
              messages: [
                {role: "system", content: "ニュースのタイトルと概要から、何が起きたか・なぜ重要かを具体的に2行で要約してください。固有名詞や数字は省略しないでください。"},
                {role: "user", content: $content}
              ]
            }' | curl -s https://models.inference.ai.azure.com/chat/completions \
              -H "Content-Type: application/json" \
              -H "Authorization: Bearer $API_KEY" \
              -d @- | jq -r '.choices[0].message.content'
          }

          NHK_SUMMARY=$(summarize "$NHK_INPUT")
          IT_SUMMARY=$(summarize "$IT_INPUT")
          GAME_SUMMARY=$(summarize "$GAME_INPUT")

          echo "NHK_SUMMARY<<EOF" >> $GITHUB_ENV
          echo "$NHK_SUMMARY" >> $GITHUB_ENV
          echo "EOF" >> $GITHUB_ENV
          echo "IT_SUMMARY<<EOF" >> $GITHUB_ENV
          echo "$IT_SUMMARY" >> $GITHUB_ENV
          echo "EOF" >> $GITHUB_ENV
          echo "GAME_SUMMARY<<EOF" >> $GITHUB_ENV
          echo "$GAME_SUMMARY" >> $GITHUB_ENV
          echo "EOF" >> $GITHUB_ENV

      # --- Send to Discord ---

      - name: Send to Discord
        env:
          WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
        run: |
          CONTENT=$(printf '📰 **今日のAI要約ニュース**\n\n🌏 **【最重要ニュース】**\n%s\n\n💻 **【AI・テック】**\n%s\n\n🎮 **【ゲーム】**\n%s' "$NHK_SUMMARY" "$IT_SUMMARY" "$GAME_SUMMARY")

          jq -n --arg content "$CONTENT" '{content: $content}' | curl -s \
            -H "Content-Type: application/json" \
            -X POST \
            -d @- \
            "$WEBHOOK"